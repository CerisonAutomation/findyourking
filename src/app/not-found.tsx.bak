import Link from 'next/link'
import { Button } from '@/components/ui/button'
import { Search, Compass, MessageCircle, User } from 'lucide-react'

export default function NotFound() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-background p-4 relative overflow-hidden">
      {/* Background Ambience */}
      <div className="absolute top-[-10%] right-[-10%] w-[50%] h-[50%] bg-primary/10 blur-[120px] rounded-full pointer-events-none" />
      <div className="absolute bottom-[-10%] left-[-10%] w-[40%] h-[40%] bg-accent/10 blur-[100px] rounded-full pointer-events-none" />

      <div className="w-full max-w-3xl text-center space-y-12 relative z-10">
        <div className="space-y-4">
          <h1 className="text-9xl font-black tracking-tighter text-transparent bg-clip-text bg-gradient-to-b from-white to-white/20 select-none">
            404
          </h1>
          <p className="text-2xl font-medium text-muted-foreground">
            This coordinate does not exist in the Find Your King multiverse.
          </p>
        </div>

        <div className="space-y-6">
          <p className="text-muted-foreground max-w-md mx-auto">
            The page you're looking for has vanished into the void, or perhaps never existed at all.
          </p>

          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <Button asChild size="lg" className="bg-gradient-to-r from-primary to-purple-600 hover:from-primary/90 hover:to-purple-600/90">
              <Link href="/discover">
                <Compass className="mr-2 h-5 w-5" />
                Discover People
              </Link>
            </Button>
            
            <Button variant="outline" size="lg" asChild>
              <Link href="/">
                <Search className="mr-2 h-5 w-5" />
                Go Home
              </Link>
            </Button>
          </div>
        </div>

        <div className="grid grid-cols-1 sm:grid-cols-3 gap-8 max-w-2xl mx-auto">
          <div className="text-center space-y-2">
            <div className="w-12 h-12 bg-primary/10 rounded-full flex items-center justify-center mx-auto">
              <Search className="h-6 w-6 text-primary" />
            </div>
            <h3 className="font-semibold">Discover</h3>
            <p className="text-sm text-muted-foreground">
              Find amazing people in your area
            </p>
          </div>

          <div className="text-center space-y-2">
            <div className="w-12 h-12 bg-primary/10 rounded-full flex items-center justify-center mx-auto">
              <MessageCircle className="h-6 w-6 text-primary" />
            </div>
            <h3 className="font-semibold">Connect</h3>
            <p className="text-sm text-muted-foreground">
              Start meaningful conversations
            </p>
          </div>

          <div className="text-center space-y-2">
            <div className="w-12 h-12 bg-primary/10 rounded-full flex items-center justify-center mx-auto">
              <User className="h-6 w-6 text-primary" />
            </div>
            <h3 className="font-semibold">Match</h3>
            <p className="text-sm text-muted-foreground">
              Find your perfect connection
            </p>
          </div>
        </div>
      </div>
    </div>
  )
}